삼항 연산자
	 참 if 조건식 else 거짓

	1. 값1 if 조건식 else 값2
	2. 문장1 if 조건식 else 문장2

"{d:.2f}".format(d=3.1561)
	소수점 둘째자리까지 자른다.
------------------------------------------------------------------
제어문
	조건문
		if문
if 조건식 :
	실행할 문장
if 조건식 :
	실행할 문장
if 조건식 :
	실행할 문장
------------------------------------------------------------------
if 조건식 :
	실행할 문장
elif 조건식 :
	실행할 문장
else :
	실행할 문장
	반복문


3항연산자보다 if 조건문보다 빠르지만 연산이 많기 때문에 메모리를 더 많이 사용한다.
if문은 3항 연산자보다 느리지만 메모리를 적게 차지한다.
------------------------------------------------------------------
대입 연산자(복합 대입 연산자, 누적 연산자)
+=, -=, *=, /=, %=, //=,....
	
	money = 10000
	#money = money - 500
	money -= 500

------------------------------------------------------------------
	반복문
		for문

	for 변수명 in range(초기값, 끝값, 증감값) :
		반복할 문장

		while문











